# v0.6.5 Graph Acceptance Baseline Release Notes

`v0.6.5 Graph Acceptance Baseline` records the relationship-graph evidence
needed before v0.7 adaptive research changes routing and progress behavior.

## Included

- Published reviewer-safe summary of the offline relationship-graph acceptance
  gate.
- Published optional live judged smoke results for four relationship-heavy
  flow/change records with required graph expansion.
- Package metadata and OpenAPI version advance to `0.6.5`.

## Evidence

- Offline graph checks passed with nodes, edges, and relationship counts.
- Self-repository graph on `dev` commit
  `85031c15e20ae22ec62c2bce432c73b4a3da35a9` had 1,533 nodes and 5,857 edges.
- Optional judged smoke expanded the graph for 4/4 records.
- Judged smoke averages: correctness 3.625/5, reference coverage 3.5/5,
  citation precision 5.0/5, faithfulness 5.0/5, unsupported claims 0, and
  average total latency 38.0 seconds.

## Scope

This release does not add new product behavior. It publishes the graph
acceptance baseline that v0.7 adaptive routing will build on.

## Release Checks

- Package metadata and OpenAPI report `0.6.5`.
- `make evaluate-relationship-graph` passes with Qdrant/PostgreSQL healthy.
- Optional `--run-answers --require-graph-expansion` smoke passes when
  `OPENAI_API_KEY` is available.
- `make test-all` remains the broad local quality gate.
